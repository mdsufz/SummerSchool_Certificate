[project]
name = "summerschool-certificate"
version = "0.1.0"
description = "Generate attendance and grade certificates for the UFZ Summer School"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "polars>=1.32.3",
    "reportlab>=4.4.3",
    "xlsxwriter>=3.2.5",
]
